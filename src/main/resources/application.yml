spring:
  datasource:
    url: jdbc:mysql://localhost:3306/user?serverTimezone=Europe%2FIstanbul
    username: {username}
    password: {password}
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    hibernate:
      ddl-auto: create
    show-sql: true
